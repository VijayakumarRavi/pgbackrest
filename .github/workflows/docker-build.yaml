name: Build and Publish Postgres

on:
  push:
    branches: ['master']
    paths:
      - Dockerfile
      - docker-bake.hcl
      - entrypoint.sh
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push
        run: |
          docker buildx bake \
            --push \
            --load=false \
            --progress=plain \
            --file docker-bake.hcl \
            --set *.labels.org.opencontainers.image.revision=${{ github.sha }} \
            --set *.labels.org.opencontainers.image.source=${{ github.repositoryUrl }}
